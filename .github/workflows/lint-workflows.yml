# on:
#   push:
#     paths:
#       - '.github/workflows/*'
#
# jobs:
#   lint:
#     timeout-minutes: 5
#     uses: sasaplus1/reusable-workflows/.github/workflows/lint-workflows.yml

name: lint workflows

on:
  workflow_call:

jobs:
  lint-workflow-files:
    name: lint workflow files
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: docker://rhysd/actionlint:latest
